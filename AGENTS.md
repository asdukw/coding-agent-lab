# 项目约定

- 默认不主动运行测试。仅当用户尝试发版时，提示用户并询问是否需要运行测试；获得用户确认后再执行。
- Git 提交策略：当一个独立、可验证的工作单元完成并通过适当检查后，自动创建 commit，无需等待用户再次指示。只暂存当前任务直接涉及的文件或精确 hunk，保留工作区中的其他未提交改动；工作未完成、检查失败、提交边界不清晰或用户明确要求不提交时，不自动提交。提交信息使用简洁、描述性的 Conventional Commit 格式。
